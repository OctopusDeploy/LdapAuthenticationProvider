name: Build, Test, Package and Push

# Controls when the action will run. 
on: 
  # Triggers the workflow on pull request events and merges/pushes to main
  push:
    branches:
      - main
      - release/*
  pull_request:
    types: [opened, synchronize, reopened]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0 # all
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 5.0.x
          
      - name: Nuke Build 🏗
        id: build
        shell: bash
        # Pass branch and patch number to Nuke OctoVersion 
        # (for pull_request events we override the /refs/pull/xx/merge branch to the PR's head branch)
        run: |
          ./build.sh \
            --OctoVersionBranch ${{ github.head_ref || github.ref }} \
            --OctoVersionPatch ${{ github.run_number }}

      - name: Tag release (when not pre-release) 🏷️
        if: ${{ !contains( steps.build.outputs.octoversion_fullsemver, '-' ) }}
        uses: actions/github-script@v3
        with:
          github-token: ${{ github.token }}
          script: |
            github.git.createRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: "refs/tags/${{ steps.build.outputs.octoversion_fullsemver }}",
              sha: context.sha
            })

      - name: Install Octopus CLI 🐙
        uses: OctopusDeploy/install-octopus-cli-action@v1.1.7
        with:
          version: latest
      
      - name: Push to Octopus 🐙
        uses: OctopusDeploy/push-package-action@v1.1.1
        with:
          server: https://deploy.octopus.app
          space: Integrations
          api_key: ${{ secrets.DEPLOY_API_KEY }}
          packages: |
            ./artifacts/Octopus.Client.Extensibility.Authentication.Ldap.${{ steps.build.outputs.octoversion_nugetversion }}.nupkg
            ./artifacts/Octopus.Server.Extensibility.Authentication.Ldap.${{ steps.build.outputs.octoversion_nugetversion }}.nupkg

      - name: Create Release in Octopus 🐙
        uses: OctopusDeploy/create-release-action@v1.1.1
        with:
          server: https://deploy.octopus.app
          space: Integrations
          api_key: ${{ secrets.DEPLOY_API_KEY }}
          project: "LdapAuthenticationProvider"
          packages: |
            Octopus.Client.Extensibility.Authentication.Ldap:${{ steps.build.outputs.octoversion_nugetversion }}
            Octopus.Server.Extensibility.Authentication.Ldap:${{ steps.build.outputs.octoversion_nugetversion }}
